fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
# Windows only (optional — enables real Windows Event Log reading):
pywin32>=306
# Optional — enables CPU/RAM/disk metrics:
psutil>=5.9
# Optional — enables screenshot capture:
mss>=10.0
Pillow>=10.0
